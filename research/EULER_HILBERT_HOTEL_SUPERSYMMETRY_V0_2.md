# Euler-Hilbert-Hotel Supersymmetry v0.2

Status: **EXACT OPERATOR CONSTRUCTION / STANDARD TOEPLITZ-FREDHOLM INDEX THEORY / EXACT PAULI-TETRAHEDRAL ORBIT / BINARY-TETRAHEDRAL COVARIANCE / PHYSICAL INTERPRETATION OPEN**

Date: 2026-09-22

## 1. Separation principle

This revision treats supersymmetry as an independent mathematical programme. It does not require a Riemann-hypothesis route, a theory-of-everything claim, or any other external programme.

There are two separate theorem bricks:

1. **Euler-Toeplitz-Hilbert-Hotel SUSY index theorem** -- a 0+1 dimensional N=2 supersymmetric operator theorem.
2. **Pauli-binary-tetrahedral supertranslation theorem** -- a 3+1 dimensional N=1 supertranslation realization on a tetrahedral null frame after explicit CAR input.

No theorem below requires the two bricks to be identified.

## 2. General Fredholm SUSY factorization

Let A:H_+ -> H_- be a bounded Fredholm operator. On H_+ direct-sum H_- define

[
Gamma=diag(I,-I),
quad
Q_A=
\begin{pmatrix}0&0\\A&0\end{pmatrix},
quad
Q_A^dagger=
\begin{pmatrix}0&A^dagger\\0&0\end{pmatrix}.
]

Then

[
Q_A^2=(Q_A^dagger)^2=0,
qquad
{Gamma,Q_A}={Gamma,Q_A^dagger}=0,
]

and

[
H_A={Q_A,Q_A^dagger}
=
diag(A^dagger A,AA^dagger).
]

For the real supercharges Q_1=Q_A+Q_A^dagger and Q_2=i(Q_A^dagger-Q_A),

[
{Q_i,Q_j}=2 delta_ij H_A.
]

The zero modes are

[
ker H_+=ker A,
qquad
ker H_-=ker A^dagger.
]

Therefore, with the displayed grading convention,

[
oxed{
Delta_W
=
dim ker A-dim ker A^dagger
=
ind(A).
}
]

All positive-energy states pair across the two grading sectors. Only the Fredholm defect remains unpaired.

## 3. Euler-Toeplitz-Hilbert-Hotel index theorem

Let f:S^1 -> C\{0} be continuous and let T_f be its Toeplitz operator on Hardy space H^2(D). The classical Toeplitz index theorem gives

[
ind(T_f)=-wind(f,0).
]

Combining with the Fredholm SUSY factorization,

[
oxed{
Delta_W(T_f)
=
-wind(f,0).
}
]

For a smooth unit-modulus phase f(e^{i theta})=e^{i phi(theta)},

[
oxed{
Delta_W
=
-rac{phi(2pi)-phi(0)}{2pi}.
}
]

Thus the supersymmetric index is the integer topological charge of the Euler phase.

### Hilbert-Hotel specialization

For f(z)=z^k,

[
T_f=S^k,
qquad
wind(z^k)=k,
]

hence

[
oxed{
Delta_W
=
ind(S^k)
=
-k.
}
]

The Hilbert-Hotel shift defect is therefore the monomial specialization of the Toeplitz winding index.

## 4. Holomorphic zero-count corollary

Let

[
B(z)=e^{i alpha}
\prod_{j=1}^r
\left(
\frac{z-a_j}{1-\bar a_j z}
\right)^{m_j},
qquad |a_j|<1,
]

be a finite Blaschke product of total degree

[
k=\sum_j m_j.
]

Then B is inner, T_B=M_B is an isometry, and

[
dim(H^2 \ominus BH^2)=k.
]

Therefore

[
oxed{
\# Z_D(B)
=
wind(B)
=
-ind(T_B)
=
-Delta_W,
}
]

with zeros counted with multiplicity.

The unpaired supersymmetric zero-mode space is exactly the model space

[
K_B=H^2\ominus BH^2.
]

For distinct zeros, and with the standard derivative generalization for multiplicities, this space is generated by the Hardy reproducing kernels associated with the zeros. Thus analytic zero multiplicity becomes a protected zero-mode multiplicity in this exact operator model.

## 5. Topological protection and composition

If f_t:S^1 -> C\{0} is a continuous homotopy of nonvanishing symbols, then

[
wind(f_t)=constant
]

and therefore

[
oxed{Delta_W(T_{f_t})=constant.}
]

Changing the protected zero-mode imbalance requires crossing the non-Fredholm boundary, i.e. the symbol must hit zero on S^1.

Likewise compact perturbations preserve the Fredholm index whenever Fredholmness is retained.

At the index level,

[
oxed{
Delta_W(fg)
=
Delta_W(f)+Delta_W(g),
}
]

because wind(fg)=wind(f)+wind(g). This is the modular composition law for independent phase defects.

## 6. Tetrahedron as the Pauli/SIC orbit

For the four Bloch vectors

[
n_1=(1,1,1)/sqrt3,
quad
n_2=(1,-1,-1)/sqrt3,
]

[
n_3=(-1,1,-1)/sqrt3,
quad
n_4=(-1,-1,1)/sqrt3,
]

define rank-one projectors

[
P_a=\frac12(I+n_a\cdot sigma).
]

Then

[
sum_a P_a=2I,
qquad
Tr(P_aP_b)=1/3
quad(a != b).
]

Hence E_a=P_a/2 is the qubit tetrahedral SIC-POVM: four outcomes, which is the minimal informationally complete number d^2 for d=2.

The tetrahedron is also exactly the projective Pauli orbit of P_1:

[
{P_1,P_2,P_3,P_4}
=
{U P_1 U^dagger:
U in {I,sigma_x,sigma_y,sigma_z}}.
]

Projectively this is the V4 action. Its determinant-one spin lift is precisely

[
Q_8=
{+/-I,+/-i sigma_x,+/-i sigma_y,+/-i sigma_z},
]

the same Q8/V4 structure proved in SOH-G013.

This answers the non-arbitrariness question: the tetrahedron is simultaneously the regular 2-design/SIC frame and the orbit of the Pauli projective group.

## 7. Binary-tetrahedral completion

Define

[
U_3
=
-\frac12 I
-\frac{i}{2}(sigma_x+sigma_y+sigma_z).
]

Then

[
U_3^3=I
]

and conjugation by U_3 cyclically permutes the Pauli axes. The group generated by Q8 and U_3 is the binary tetrahedral group

[
2T ~= Q8 semidirect C3,
qquad |2T|=24.
]

Its projective image is the orientation-preserving tetrahedral group

[
2T/{+/-I} ~= A4.
]

Thus the earlier Q8 lift is not an isolated algebra: it is the normal quaternionic core of the full spinorial tetrahedral symmetry.

## 8. Tetrahedral N=1 supertranslation algebra

Let each null ray k_a=(1,n_a) factor as

[
k_{a,alpha dot-alpha}
=
lambda_{a,alpha}
\bar lambda_{a,dot-alpha}.
]

Attach four fermionic modes obeying

[
{f_a,f_b^dagger}=delta_ab,
qquad
{f_a,f_b}=0.
]

For p_a>=0 define

[
P_{alpha dot-beta}
=
sum_a p_a lambda_{a,alpha}
\bar lambda_{a,dot-beta},
]

[
Q_alpha
=
sqrt2 sum_a sqrt(p_a)
lambda_{a,alpha} f_a^dagger.
]

Then

[
oxed{
{Q_alpha,\bar Q_{dot-beta}}
=
2P_{alpha dot-beta},
}
]

with same-chirality anticommutators equal to zero.

This is an exact N=1 supertranslation realization once the CAR modes are supplied.

## 9. Binary-tetrahedral covariance

For g in 2T, the spin action permutes tetrahedral rays projectively:

[
g lambda_a
=
e^{i chi_{g,a}}
lambda_{pi_g(a)}.
]

Because the CAR algebra is invariant under unitary changes of fermionic basis, choose its lift so that

[
V_g f_a^dagger V_g^{-1}
=
e^{-i chi_{g,a}}
f_{pi_g(a)}^dagger.
]

Then the geometric supercharge tensor

[
Q(p)
=
sqrt2 sum_a sqrt(p_a)
lambda_a tensor f_a^dagger
]

is equivariant:

[
oxed{
(g tensor V_g)Q(p)
=
Q(pi_g p).
}
]

For equal tetrahedral weights it is invariant under the simultaneous binary-tetrahedral action. The supertranslation anticommutator is covariant under the same action.

## 10. Status

### EXACT / STANDARD THEOREM-LEVEL IN THE DECLARED MODEL

- Fredholm operator -> N=2 SUSY factorization.
- Witten index = Fredholm index.
- Toeplitz index = minus winding for nonvanishing continuous symbols.
- Euler phase winding = supersymmetric index.
- S^k Hilbert-Hotel defect = -k.
- finite Blaschke zero count = protected SUSY zero-mode count.
- homotopy and compact-perturbation protection of the index.
- tetrahedral SIC identities.
- Pauli/V4 orbit of the tetrahedron.
- Q8 normal core and binary-tetrahedral completion.
- tetrahedral N=1 supertranslation algebra after explicit CAR input.
- binary-tetrahedral covariance after the corresponding CAR basis lift.

### NOT CLAIMED

- that any of the standard component theorems are newly discovered;
- that a bare complex plane forces a unique supercharge;
- that tetrahedral geometry derives CAR fermions without extra input;
- that the mathematical SUSY constructions imply supersymmetry is realized in Nature.

The intended contribution is the exact synthesis and separation of the operator-index, analytic-zero, Pauli/SIC, and tetrahedral-spinor structures into independently checkable supersymmetry modules.
